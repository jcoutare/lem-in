#include "lemin.h"
#include "libft.h"
#include <unistd.h>

l_room	*lst_copy(l_room **begin_list)
{
  l_room *new;

  new = NULL;
  while (*begin_list)
    {
      if (!(l_push_back(&new, (*begin_list)->name)))
	return (NULL);
      (*begin_list) = (*begin_list)->next;
    }
  return (new);
}
